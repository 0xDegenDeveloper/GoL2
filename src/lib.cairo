mod utils {
    mod constants;
    mod life_rules;
    mod packing;
    mod svg;
    mod uri;
    mod whitelist;
    mod test_contract;
}

mod scripts { // mod deploy_script;
}

mod contracts {
    mod gol;
    mod nft;
}
