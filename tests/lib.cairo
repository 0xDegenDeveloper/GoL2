mod utils {
    mod _life_rules;
    mod _math;
    mod _packing;
}

mod contracts {
    mod gol {
        mod _gol;
        mod gol_internal;
        mod gol_ownable;
        mod gol_upgradeable;
    }
}

mod forking {
    mod goerli_fork;
    mod mainnet_fork;
}
